# Custom partition table for WT32-SC01 Plus (16MB flash) - OTA-capable
# (otadata + two app slots, no "factory" partition) so the web UI can
# install updates (manual .bin upload or from a GitHub release) without a
# separate recovery image.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x6000,
otadata,    data, ota,      0xf000,   0x2000,
phy_init,   data, phy,      0x11000,  0x1000,
ota_0,      app,  ota_0,    0x20000,  0x300000,
ota_1,      app,  ota_1,    0x320000, 0x300000,
# ~9.8MB of the 16MB flash is intentionally left unpartitioned for future use
# (e.g. a data/storage partition).
